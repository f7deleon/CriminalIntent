<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    >

    <TextView
        android:id="@+id/txtCrimeTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="left"
        android:layout_marginStart="8dp"
        app:layout_constraintStart_toStartOf="parent"
        tools:text="Crime Title"
        />

    <TextView
        android:id="@+id/txtCrimeDate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="end"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/txtCrimeTitle"
        tools:text="Crime Date"
        />

    <ImageView
        android:id="@+id/imgSolvedCrime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:foregroundGravity="right"
        android:layout_marginEnd="8dp"
        android:src="@drawable/ic_solved"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        tools:layout_editor_absoluteY="5dp"
        />

    <ImageView
        android:id="@+id/imgPoliceRequired"
        android:layout_width="34dp"
        android:layout_height="38dp"
        android:layout_marginEnd="8dp"
        android:visibility="gone"
        app:layout_constraintEnd_toStartOf="@+id/imgSolvedCrime"
        app:srcCompat="@drawable/police_badge"
        tools:layout_editor_absoluteY="0dp"
        />

</android.support.constraint.ConstraintLayout>
